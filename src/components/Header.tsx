import React from 'react';

import '../styles/header.scss';

export const Header: React.FC = () => {
    return (
        <header>
            <h1>React - Atividade Prévia</h1>
        </header>
    )
}